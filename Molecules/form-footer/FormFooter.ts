export default class FormFooter {
  constructor(private container: HTMLElement, private formClickables: HTMLElement, private formSubmitButton: HTMLElement, private formLink: HTMLElement, private switchLink: HTMLElement) {
  }
}
